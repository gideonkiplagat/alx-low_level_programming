#include <stdio.h>

/**
 * main - Entry point
 * Do not use puts
 * compile with warning when using -wall gcc
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
    printf("with proper grammar, but the outcome is a piece of art,\n");
    return (0);
}
